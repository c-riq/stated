
import {describe, expect, it} from '@jest/globals';
const {checkRequiredObservations} = jest.requireActual("./poll");

describe('checkRequiredObservations', () => {
    it('should return true when there is a valid observation', () => {
        
    });
});
