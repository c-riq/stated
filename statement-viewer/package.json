{
  "name": "statement-viewer",
  "version": "1.0.0",
  "type": "module",
  "description": "Static web application for viewing Stated protocol statements",
  "scripts": {
    "start": "npm run build && node dist/server.js",
    "build": "tsc",
    "watch": "tsc --watch",
    "gen": "npm run build && node dist/generate-samples.js",
    "update-lib": "cd ../stated-protocol-parser && npm run build && cd ../statement-viewer && rm -rf lib && mkdir -p lib && cp -r ../stated-protocol-parser/dist/esm/* lib/"
  },
  "keywords": [
    "stated",
    "protocol",
    "statements",
    "viewer"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "stated-protocol-parser": "file:../stated-protocol-parser",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0",
    "@types/express": "^4.17.21"
  }
}