/** localStorage key for the API key (shared across all editors) */
export const API_KEY_STORAGE_KEY = 'stated_api_key';

/** localStorage key for the cached config */
export const CONFIG_CACHE_KEY = 'stated_config_cache';